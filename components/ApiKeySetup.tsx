import React from 'react';

// Este componente é um placeholder para corrigir um erro de compilação
// que ocorria quando o arquivo estava vazio. Ele não renderiza nada.
const ApiKeySetup: React.FC = () => {
  return null;
};

export default ApiKeySetup;
